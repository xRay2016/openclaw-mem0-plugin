{
  "id": "openclaw-mem0-plugin",
  "name": "Mem0 OpenClaw Plugin",
  "description": "Mem0 search + add memory via lifecycle hooks",
  "version": "0.1.0",
  "kind": "lifecycle",
  "main": "./index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "baseUrl": {
        "type": "string",
        "description": "Mem0 API Base URL",
        "default": "https://api.mem0.ai"
      },
      "apiKey": {
        "type": "string",
        "description": "Mem0 API Key (Token auth; supports ~/.openclaw/.env, ~/.moltbot/.env, ~/.clawdbot/.env; falls back to process env)"
      },
      "userId": {
        "type": "string",
        "description": "Mem0 user_id (default: openclaw-user)",
        "default": "openclaw-user"
      },
      "searchEnabled": {
        "type": "boolean",
        "description": "Enable search memory search (default: true)",
        "default": true
      },
      "addEnabled": {
        "type": "boolean",
        "description": "Enable add memory (default: true)",
        "default": true
      }
    },
    "additionalProperties": false
  }
}